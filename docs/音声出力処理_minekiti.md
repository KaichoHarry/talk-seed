# AI会話支援システム 音声変換機能 要件定義書

---

## 1. システム概要

待ち時間中の利用者同士の会話を支援するため、AIが会話へ参加し話題提供や会話補助を行う。

本担当機能では、AIが生成した発話テキストを自然な音声へ変換し、スマートフォンのWebブラウザ上で再生する機能を開発する。

---

## 2. 利用者定義（アクター）

| アクター | 説明 | できる操作 |
|----------|------|-----------|
| 利用者 | システム利用者 | 音声設定変更、音声再生 |
| AI会話生成機能 | AIによる会話生成モジュール | 発話テキスト送信 |
| 音声変換機能 | 本担当機能 | テキストを音声へ変換 |

---

## 3. ユースケース一覧

| # | ユースケース名 | アクター | 概要 |
|---|---|---|---|
| UC1 | 音声生成 | AI会話生成機能 | AI発話テキストを音声化する |
| UC2 | 音声再生 | 利用者 | 音声を再生する |
| UC3 | 音声変更 | 利用者 | 音声タイプを変更する |
| UC4 | 音量変更 | 利用者 | 音量を変更する |
| UC5 | 話速変更 | 利用者 | 話す速度を変更する |

---

## 4. 機能要件

### 4-1. 音声生成機能

- [ ] AI生成テキストを受信する
- [ ] テキストを音声データへ変換する
- [ ] 音声生成結果を返却する

### 4-2. 音声設定機能

- [ ] 音声タイプ変更
- [ ] 話速変更
- [ ] 音量変更

### 4-3. 音声再生機能

- [ ] 生成音声を再生する
- [ ] 再生停止ができる
- [ ] 再生終了を通知する

---

## 5. 非機能要件

### 5-1. 性能

- [ ] 音声生成時間は3秒以内
- [ ] 音声再生遅延を最小限にする

### 5-2. 可用性

- [ ] 音声生成失敗時もシステムを停止させない
- [ ] 音声未生成時はエラーメッセージを表示する

### 5-3. 操作性

- [ ] スマートフォンから操作可能
- [ ] ブラウザのみで利用可能
- [ ] インストール不要

---

## 6. 制約条件

| 項目 | 内容 |
|--------|--------|
| 実装言語 | Python / TypeScript |
| バックエンド | Flask |
| フロントエンド | React |
| 音声合成 | edge-tts（サーバーサイドでmp3を生成しbase64で返却） |
| 実行環境 | スマートフォンWebブラウザ |
| 通信方式 | HTTP/HTTPS |

---

## 7. ソフトウェア要件と機能の対応

| 要件 | 対応機能 |
|--------|--------|
| 音声生成 | generate |
| 音声再生 | play |
| 音声停止 | stop |
| 音声変更 | change_voice |
| 音量変更 | set_volume |
| 話速変更 | set_rate |

---

## 8. 機能詳細

### 8-1. generate（音声生成）

- [ ] アクター：AI会話生成機能
- [ ] 概要：AI生成テキストを音声へ変換

#### 入力

- text

#### 出力

- 音声データ

---

### 8-2. play（音声再生）

- [ ] アクター：利用者
- [ ] 概要：生成音声を再生

#### 出力

- 音声再生開始

---

### 8-3. stop（音声停止）

- [ ] アクター：利用者
- [ ] 概要：再生中の音声を停止

---

### 8-4. change_voice（音声変更）

- [ ] アクター：利用者
- [ ] 概要：音声タイプを変更

#### 選択肢

- 女性音声
- 男性音声
- ロボット音声

---

### 8-5. set_volume（音量変更）

- [ ] アクター：利用者
- [ ] 概要：音量を変更

#### 範囲

0.0 ～ 1.0

---

### 8-6. set_rate（話速変更）

- [ ] アクター：利用者
- [ ] 概要：話す速度を変更

#### 範囲

0.5 ～ 2.0

---

## 9. 入出力仕様

### 入力

| 項目 | 型 | 説明 |
|--------|--------|--------|
| text | string | AI生成テキスト |
| voice | string | 音声タイプ |
| volume | float | 音量 |
| rate | float | 話速 |

### 出力

| 項目 | 型 | 説明 |
|--------|--------|--------|
| audio | audio | 音声データ |
| status | string | 処理結果 |

---

## 10. 関数一覧

| 関数名 | 引数 | 戻り値 | 処理概要 |
|---------|---------|---------|---------|
| generate_speech | text | audio | 音声生成 |
| play_audio | audio | None | 音声再生 |
| stop_audio | なし | None | 音声停止 |
| change_voice | voice | None | 音声変更 |
| set_volume | volume | None | 音量変更 |
| set_rate | rate | None | 話速変更 |

---

## 11. 実装メモ（サーバー/クライアントの役割分担）

実装では上記の関数群を以下のように分担している。

| 関数 | 実装場所 | 実装 |
|---|---|---|
| generate_speech | サーバー (`backend/voice/service.py: synthesize_speech`) | edge-ttsで音声合成し、`POST /voice/speak`でbase64のmp3を返す |
| change_voice | サーバー | `voice_type`("female"/"male"/"robot")に応じて音声・ピッチ・話速のプロファイルを切り替える |
| play_audio / stop_audio | クライアント (`frontend/src/App.tsx: speak`) | 受け取ったbase64をdata URLにし、ブラウザの`Audio`要素で再生する |
| set_volume / set_rate | クライアント | `Audio.volume` / `Audio.playbackRate` に音声設定画面の値を反映する |